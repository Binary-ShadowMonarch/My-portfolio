#!/bin/bash

# Navigate to the directory where your site files are hosted
cd C:\Users\Zenith\Documents\GitHub\"My Portfolio"

# Pull the latest changes from GitHub
git pull origin main

# Optionally, run any build commands (e.g., if you're using a static site generator)
# npm install
# npm run build

# Restart the web server (if necessary)
# systemctl restart apache2

# You may need to change ownership/permissions of files, depending on your setup

